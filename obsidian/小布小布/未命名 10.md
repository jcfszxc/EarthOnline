

```shell
conda env remove -n pcdet_new -y

conda create -n pcdet_new python=3.8 -y
conda activate pcdet_new
pip install nuscenes-devkit numba SharedArray
conda install -c nvidia cuda-toolkit=11.3
pip install torch==1.12.1+cu113 torchvision==0.13.1+cu113 --extra-index-url https://download.pytorch.org/whl/cu113

cd /data/zxc/Projects/1.ATTACK_SYSTEM/temp_0/third_party/OpenPCDet

export CC=/usr/bin/gcc
export CXX=/usr/bin/g++
pip install -e . --no-build-isolation

cd ../../

pip install spconv-cu113
python 0_kitti_all_in_one_shuiwu.py

```



```shell
conda env remove -n pcdet_5090 -y

conda create -n pcdet_5090 python=3.10 -y
conda activate pcdet_5090

conda install -c nvidia cuda-toolkit=12.8

%% pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
pip3 install torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu126 %%

%% pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121 %%


pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu128

pip install nuscenes-devkit numba SharedArray

export CC=/usr/bin/gcc
export CXX=/usr/bin/g++
cd /data/zxc/Projects/1.ATTACK_SYSTEM/temp_0/third_party/OpenPCDet
pip install -e . --no-build-isolation

pip install spconv-cu128

python 0_kitti_all_in_one_shuiwu.py

```


```
cd /tmp
rm -rf cumm spconv 
pip uninstall cumm spconv pccm -y 
rm -rf ~/.cache/pip/wheels/*cumm*

git clone https://github.com/FindDefinition/cumm.git


cd /tmp/cumm # 尝试直接运行 pccm 命令来查看状态或强制构建 # pccm 通常会自动检测 setup.py 中的配置 python -m pccm --help # 尝试清理 pccm 缓存 rm -rf .pccm_cache rm -rf build rm -rf cumm/build # 关键：尝试使用 pip install -v (verbose) 模式，这会强制显示所有底层命令 # 这比 setup.py 更可靠 pip install -v -e . --no-build-isolation


python -c " import sys sys.path.insert(0, '.') # 确保加载当前目录的 cumm import cumm print(f'Cumm loaded from: {cumm.__file__}') print(f'Success!') "


cd /tmp python -c " import cumm print(f'Cumm Path: {cumm.__file__}') from cumm.constants import CUMM_CPU_ONLY_BUILD print('✅ Global Import Success!') "

```


```
cd /tmp rm -rf spconv git clone https://github.com/traveller59/spconv.git cd spconv git submodule update --init --recursive # 设置同样的架构环境变量 (以防 spconv 也需要显式指定) export TORCH_CUDA_ARCH_LIST="8.0;8.6;9.0;12.0" # 使用 verbose 模式安装 spconv pip install -v -e . --no-build-isolation



```


```
conda env remove -n pcdet_5090 -y

conda create -n pcdet_5090 python=3.10 -y
conda activate pcdet_5090

conda install -c nvidia cuda-toolkit=12.8 -y
nvcc --version

pip install torch==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu128

python -c "import torch; print(torch.__version__); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"

pip install nuscenes-devkit numba SharedArray

export CC=/usr/bin/gcc
export CXX=/usr/bin/g++
cd /data/zxc/Projects/1.ATTACK_SYSTEM/temp_0/third_party/OpenPCDet
pip install -e . --no-build-isolation

pip install spconv-cu128

cd ../../
python 0_kitti_all_in_one_shuiwu.py



```





